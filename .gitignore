# dependencies
node_modules/

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# env / secrets
.env
.env.*
!.env.example

# build caches
.tsup/
*.tsbuildinfo

# NOTE: dist/ is intentionally TRACKED (committed) so that
#   pnpm dlx github:reiquileut/agent-harness init
# works with no build step, regardless of the user's pnpm version.
# Do NOT add dist/ to this file.
